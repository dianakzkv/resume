<div class='nav'>
    <div class='nav-item'>
        <a href='{{href}}' class='nav-link {{#if @last}} disabled{{/if}}'>{{text}}</a>
    </div>
</div>

{{!-- <div class="col-12">
    <div class='nav'>
        {{#with navigation}}
            {{#each links}}
                <a href='{href}}' class='nav-link {#if @last}} disabled{/if}}'>{{text}}</a>
            {{/each}}
        {{/with}}
    </div>
</div> --}}